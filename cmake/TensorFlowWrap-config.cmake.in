@PACKAGE_INIT@

# Load the exported targets.
# (Support both historical and conventional export filenames.)
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/TensorFlowWrap-targets.cmake")
  include("${CMAKE_CURRENT_LIST_DIR}/TensorFlowWrap-targets.cmake")
elseif(EXISTS "${CMAKE_CURRENT_LIST_DIR}/TensorFlowWrapTargets.cmake")
  include("${CMAKE_CURRENT_LIST_DIR}/TensorFlowWrapTargets.cmake")
else()
  message(FATAL_ERROR "TensorFlowWrap targets file not found in ${CMAKE_CURRENT_LIST_DIR}")
endif()

check_required_components(TensorFlowWrap)
Then: